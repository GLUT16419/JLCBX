# 双向搜索算法问题分析

## 问题概述

双向搜索实现后，布线功能失效，无法产生任何布线结果。

---

## 详细问题分析

### 问题 1: 反向搜索没有标记节点到网格 🔴

**位置**: [`router.js` 第 556-558 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L556-L558)

**问题代码**:

```javascript
if (!backward_visited.has(current_key)) {
    backward_visited.add(current_key);
    // ❌ 缺少: sn.call(this, current, ...)
    // 反向搜索没有标记距离到网格！
```

**原始BFS的正确实现** ([`router.js` 第 631 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L631)):

```javascript
for (let node of frontier) sn.call(this, node, distance);
```

**影响**: 反向搜索只在内存中记录访问，但没有写入到`m_nodes`网格中，导致`backtrack_path`无法找到完整路径。

---

### 问题 2: 距离标记不完整 🔴

**位置**: [`router.js` 第 498-499 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L498-L499)

**问题代码**:

```javascript
forward_visited.add(current_key);
sn.call(this, current, current_g + 1); // 只标记了正向搜索的节点
```

**问题**:

- 只有正向搜索标记了节点
- 反向搜索没有标记任何节点
- 当相遇时，网格中只有部分距离数据

**回溯函数的期望** ([`router.js` 第 847-877 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L847-L877)):

```javascript
// backtrack_path 需要完整的距离场才能工作
for (let node of this.m_pcb.all_nearer_sorted(...))
```

---

### 问题 3: 过孔延迟处理逻辑错误 🔴

**位置**: [`router.js` 第 522-540 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L522-L540)

**原始BFS的过孔处理** ([`router.js` 第 650-655 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L650-L655)):

```javascript
if (new_vias_nodes.size) vias_nodes.set(distance + this.m_viascost, new_vias_nodes);
let delay_nodes = vias_nodes.get(distance); // 基于当前distance获取
if (delay_nodes !== undefined) {
	for (let node of delay_nodes) if (gn.call(this, node) === 0) new_nodes.add(node);
	vias_nodes.delete(distance);
}
```

**双向搜索的错误实现**:

```javascript
if (new_vias.size) {
	forward_vias.set(current_g + 1 + this.m_viascost, new_vias);
}
let delay_nodes = forward_vias.get(current_g + 1); // ❌ key不匹配
```

**问题**:

- 存入时用 `current_g + 1 + m_viascost`
- 取出时用 `current_g + 1`
- key永远不匹配，过孔永远不会被处理

---

### 问题 4: 终点可能没有被标记 🔴

**位置**: [`router.js` 第 603-610 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L603-L610)

**问题代码**:

```javascript
if (meeting_node) {
	for (let end of ends) {
		if (gn.call(this, end) === 0) {
			// 这个逻辑不可靠！
			sn.call(this, end, forward_g.get(end.toString()) || backward_g.get(end.toString()) + 1 || 1);
		}
	}
}
```

**问题**:

- 只有在`meeting_node`存在时才尝试标记终点
- 但如果搜索没有相遇，`meeting_node`为null，终点不会被标记
- 即使相遇，也不保证`forward_g`或`backward_g`中有终点的记录

---

### 问题 5: 相遇判断过早退出 🔴

**位置**: [`router.js` 第 492-495 行](file:///e:/kz/auto-router-plugin/iframe/jspcb/router.js#L492-L495)

**问题代码**:

```javascript
if (backward_visited.has(current_key)) {
    meeting_node = current;
    break; // 立即退出，没有完成完整的距离标记
}
```

**问题**:

- 只要检测到相遇就立即break
- 但网格上可能还有很多节点没有标记
- 回溯需要完整的距离场才能工作

---

## 对比原始BFS工作流程

### BFS正确流程:

1. **广度优先**：一层层向外扩展
2. **完整标记**：所有可达节点都标记距离
3. **终点检查**：确保所有终点都被标记才停止
4. **过孔处理**：正确的延迟机制

### 双向搜索的错误流程:

1. **同时搜索**：但只标记一半节点
2. **相遇即停**：距离场不完整
3. **终点标记**：依赖不稳定的逻辑
4. **过孔失效**：key不匹配导致过孔无法处理

---

## 修复方案建议

### 方案 1: 简单修复（推荐短期）

- 双向搜索都标记节点到网格
- 修复过孔key匹配问题
- 不提前退出，确保完整标记

### 方案 2: 简化实现（推荐）

- 先用单向A\*验证工作
- 双向搜索可以后续作为优化
- 保持代码简单可靠

### 方案 3: 完整重写

- 参考权威的双向A\*实现
- 正确处理双向距离标记
- 确保回溯能工作

---

## 当前状态

✅ **已禁用双向搜索** - 回退到A\*算法，布线恢复正常
🔄 **双向搜索代码保留** - 可后续修复

---

## 总结

双向搜索失败的根本原因：

1. ❌ 反向搜索没有标记到网格
2. ❌ 过孔key不匹配导致过孔无法处理
3. ❌ 距离场不完整导致回溯失败
4. ❌ 终点标记逻辑不可靠

建议优先使用A\*算法，它已经验证可以正常工作。
