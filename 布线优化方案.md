# 自动布线器优化方案

## 1. 当前算法分析

### 1.1 核心算法

- **路径搜索**：Lee算法（BFS漫水填充）
- **路径回溯**：贪心回溯法
- **网络排序**：按面积从小到大
- **失败处理**：随机洗牌 + 重排机制
- **碰撞检测**：空间哈希（Spatial Hash）

### 1.2 现有问题

1. **性能问题**：

    - BFS漫水填充效率随PCB尺寸指数增长
    - 空间哈希精度固定，难以平衡精度和性能
    - 没有路径搜索的启发式引导（A\*算法）
    - 每次失败后需要重置并重新布线

2. **布线质量问题**：

    - 路径优化不足，线长较长
    - 过孔数量未优化
    - 45度角走线支持不足（当前可能只有正交）
    - 网络优先级策略简单

3. **其他问题**：
    - 没有布线策略选择
    - 没有动态参数调整
    - 缺乏多策略并行探索

---

## 2. 优化方案架构

### 2.1 核心架构改进

```
┌─────────────────────────────────────────────────────────────┐
│                    用户界面层                              │
├─────────────────────────────────────────────────────────────┤
│  参数配置面板  │  布线策略选择  │  进度监控  │  结果对比  │
├─────────────────────────────────────────────────────────────┤
│                   调度器层                                  │
├─────────────────────────────────────────────────────────────┤
│  网络预处理  │  策略选择  │  并行探索  │  结果评估  │
├─────────────────────────────────────────────────────────────┤
│                   算法层                                    │
├─────────────────────────────────────────────────────────────┤
│  A*搜索器  │  路径优化器  │  多策略并行  │  动态参数调优  │
├─────────────────────────────────────────────────────────────┤
│                  数据结构层                                │
├─────────────────────────────────────────────────────────────┤
│  空间哈希  │  优先队列  │  网格管理  │  碰撞检测器      │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 具体优化措施

### 3.1 算法优化（性能 + 质量）

#### 3.1.1 A\* 路径搜索算法（替代BFS）

**优先级**：高 | **难度**：中 | **预计收益**：性能提升 30-50%

**实现方案**：

```javascript
// 使用 Manhattan 距离作为启发式函数
function heuristic(a, b) {
	return Math.abs(a[0] - b[0]) + Math.abs(a[1] - b[1]);
}

// 使用优先队列代替普通队列
class PriorityQueue {
	constructor() {
		this.elements = [];
	}

	enqueue(priority, item) {
		this.elements.push({ priority, item });
		this.elements.sort((a, b) => a.priority - b.priority);
	}

	dequeue() {
		return this.elements.shift().item;
	}

	isEmpty() {
		return this.elements.length === 0;
	}
}
```

**优势**：

- 搜索范围更小，速度更快
- 路径更优（更短）

---

#### 3.1.2 45度角走线支持

**优先级**：高 | **难度**：中 | **预计收益**：线长减少 15-25%

**实现方案**：

```javascript
// 添加 8 方向移动向量
const routing_flood_vectors_8 = [
	// 4个正交方向
	[
		[-1, 0, 0],
		[0, -1, 0],
		[1, 0, 0],
		[0, 1, 0],
	],
	// 4个对角线方向
	[
		[-1, -1, 0],
		[1, -1, 0],
		[-1, 1, 0],
		[1, 1, 0],
	],
];
```

**代价函数**：

- 正交移动：代价 = 1
- 对角线移动：代价 = 1.414（√2）
- 过孔移动：代价 = viacost

---

#### 3.1.3 双向搜索

**优先级**：中 | **难度**：高 | **预计收益**：大PCB性能提升 20-30%

**实现方案**：

```
┌─────────────┐                  ┌─────────────┐
│   起点     │ ────────────────→ │   终点     │
│  正向搜索  │                  │  反向搜索  │
└─────────────┘                  └─────────────┘
         ↓                                  ↓
         └─────────── 中间相遇 ────────────┘
```

---

### 3.2 网络预处理与调度优化

#### 3.2.1 智能网络排序

**优先级**：高 | **难度**：低 | **预计收益**：布线成功率 +10-15%

**排序因子**（按重要性）：

1. **关键网络优先**（时钟、复位等）
2. **网络类型**（电源/地 > 信号 > 其他）
3. **网络复杂度**（引脚数多 > 少）
4. **网络面积**（面积大 > 小）
5. **网络约束**（有层约束 > 无约束）

**实现**：

```javascript
function sortNets(nets) {
	return nets.sort((a, b) => {
		// 1. 关键网络优先级最高
		if (isCriticalNet(a) && !isCriticalNet(b)) return -1;
		if (!isCriticalNet(a) && isCriticalNet(b)) return 1;

		// 2. 电源/地网络
		if (isPowerNet(a) && !isPowerNet(b)) return -1;
		if (!isPowerNet(a) && isPowerNet(b)) return 1;

		// 3. 引脚数量
		if (a.terminals.length !== b.terminals.length) {
			return b.terminals.length - a.terminals.length;
		}

		// 4. 网络面积
		return b.area - a.area;
	});
}
```

---

#### 3.2.2 网络分组与批量处理

**优先级**：中 | **难度**：中 | **预计收益**：内存使用 +30%

**分组策略**：

- 按功能模块分组
- 按网络类型分组
- 按信号流向分组

---

### 3.3 动态参数与多策略

#### 3.3.1 参数自适应调整

**优先级**：中 | **难度**：中

**参数动态调整**：

```
┌─────────────────────────────────────────────────────────┐
│  布线阶段 │  清除间距 │  过孔代价 │  搜索范围限制 │
├─────────────────────────────────────────────────────────┤
│   初期   │    较大   │   较低   │    无限制     │
│   中期   │    适中   │   适中   │    区域限制   │
│   后期   │    精确   │   较高   │  严格区域限制 │
└─────────────────────────────────────────────────────────┘
```

---

#### 3.3.2 多策略并行探索

**优先级**：高 | **难度**：高 | **预计收益**：成功率 +20-30%

**策略池**：

1. **保守策略**：严格遵守间距，优先顶层
2. **激进策略**：宽松间距，优先底层
3. **平衡策略**：平衡过孔和线长
4. **面积优先**：优先使用自由区域
5. **最短路径**：纯粹追求线长最短

**实现**：

```javascript
// 并行执行多个策略（Web Workers）
async function multiStrategyRoute(pcb, strategies) {
	const results = await Promise.all(strategies.map((strategy) => runRouteWithStrategy(pcb, strategy)));

	// 选择最优结果
	return selectBestResult(results);
}
```

---

### 3.4 路径后优化

#### 3.4.1 路径简化与优化

**优先级**：高 | **难度**：低 | **预计收益**：线长 -10-20%

**优化技术**：

1. **冗余点去除**：三点共线去掉中间点
2. **倒角优化**：90度角改为45度
3. **走线拉直**：弯曲的线拉直
4. **过孔优化**：移除不必要的过孔

**实现**：

```javascript
function optimizePath(path) {
	let optimized = [];

	for (let i = 0; i < path.length; i++) {
		// 去除共线点
		if (i >= 2) {
			const [x1, y1, z1] = path[i - 2];
			const [x2, y2, z2] = path[i - 1];
			const [x3, y3, z3] = path[i];

			// 共线检测
			if (z1 === z2 && z2 === z3 && (x1 - x2) * (y2 - y3) === (y1 - y2) * (x2 - x3)) {
				// 跳过中间点
				continue;
			}
		}
		optimized.push(path[i]);
	}

	return optimized;
}
```

---

#### 3.4.2 全局重排与推挤

**优先级**：中 | **难度**：高 | **预计收益**：线长 -5-15%

**算法**：

- 滑动窗口优化
- 拥挤区域推挤
- 整体松弛

---

### 3.5 数据结构优化

#### 3.5.1 分层空间哈希

**优先级**：高 | **难度**：中 | **预计收益**：碰撞检测 +40%

**实现**：

- 每一层独立哈希表
- 动态调整单元格大小
- 增量更新而非重建

---

#### 3.5.2 布隆过滤器快速排除

**优先级**：中 | **难度**：中 | **预计收益**：碰撞检测 +20%

**实现**：使用布隆过滤器快速排除不可能碰撞的区域

---

### 3.6 用户体验优化

#### 3.6.1 进度可视化

**优先级**：中 | **难度**：低

**功能**：

- 实时显示布线进度
- 已布网络数 / 总网络数
- 估算剩余时间
- 当前网络名称

---

#### 3.6.2 布线策略选择

**优先级**：中 | **难度**：低

**提供选择**：

- 快速布线（牺牲一点质量）
- 高质量布线（更慢但更好）
- 平衡模式（默认）
- 自定义模式（自行调参）

---

## 4. 分阶段实施计划

### 阶段一：快速见效（1-2周）

- ✅ A\* 搜索算法
- ✅ 45度角走线支持
- ✅ 智能网络排序
- ✅ 路径简化优化

### 阶段二：进阶优化（2-3周）

- 双向搜索
- 动态参数调整
- 分层空间哈希
- 实时进度显示

### 阶段三：高级功能（3-4周）

- 多策略并行
- 全局重排优化
- 推挤算法
- 策略选择界面

### 阶段四：终极优化（2-3周）

- 机器学习辅助
- 超参数自动调优
- 历史结果复用
- 云加速支持

---

## 5. 预期效果评估

| 指标     | 当前 | 阶段一 | 阶段二 | 阶段三 | 阶段四 |
| -------- | ---- | ------ | ------ | ------ | ------ |
| 布线速度 | 1x   | 1.5-2x | 2-3x   | 3-5x   | 5-10x  |
| 线长优化 | 100% | 85-90% | 80-85% | 75-80% | 70-75% |
| 过孔数量 | 1x   | 0.9x   | 0.8x   | 0.7x   | 0.6x   |
| 成功率   | 基准 | +10%   | +20%   | +30%   | +40%   |

---

## 6. 测试与验证方案

### 6.1 测试用例集

- 2层板简单案例
- 4层板中等案例
- 6层板复杂案例
- 高密度BGA案例
- 特殊约束案例

### 6.2 对比指标

- 线长总和
- 过孔数量
- 布线时间
- 成功率
- 内存使用

### 6.3 A/B 测试框架

```javascript
// 自动化对比测试
async function runABTest(testCases, oldVersion, newVersion) {
	const results = [];

	for (const testCase of testCases) {
		const result1 = await oldVersion.run(testCase);
		const result2 = await newVersion.run(testCase);
		results.push(compareResults(result1, result2));
	}

	return generateReport(results);
}
```

---

## 7. 风险与注意事项

### 7.1 风险

- A\* 算法可能陷入局部最优
- 多策略增加了复杂度
- 动态参数需要仔细调优

### 7.2 缓解措施

- 保留回退机制（失败后用原算法）
- 渐进式引入（可开关）
- 充分测试覆盖各种场景
