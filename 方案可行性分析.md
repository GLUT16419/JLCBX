# 自动布线优化方案可行性分析

## 概述

本文档分析两个优化方案的可行性，基于现有的 API 和例程。

---

## 一、布线优化方案可行性分析

### ✅ 可行性结论：完全可行

### 1.1 核心优化项分析

| 优化项           | 可行性      | 说明                                   |
| ---------------- | ----------- | -------------------------------------- |
| A\* 路径搜索算法 | ✅ 完全可行 | 纯 JavaScript 算法实现，不依赖特殊 API |
| 45 度角走线支持  | ✅ 完全可行 | 修改搜索向量和代价函数即可             |
| 智能网络排序     | ✅ 完全可行 | 纯 JavaScript 逻辑                     |
| 路径简化优化     | ✅ 完全可行 | 纯 JavaScript 算法                     |
| 双向搜索         | ✅ 完全可行 | 纯 JavaScript 算法                     |
| 分层空间哈希     | ✅ 完全可行 | 纯 JavaScript 数据结构                 |
| 多策略并行       | ✅ 完全可行 | 使用 Web Workers（浏览器标准 API）     |
| 动态参数调整     | ✅ 完全可行 | 纯 JavaScript 逻辑                     |

### 1.2 技术实现路径

#### 1.2.1 现有代码结构分析

项目使用纯 JavaScript 实现了完整的自动布线器：

- `iframe/jspcb/router.js` - 核心布线算法
- iframe/jspcb/main.js` - 主控制逻辑
- `iframe/jspcb/index.html` - 用户界面
- `iframe/jspcb/dsn2pcb.js` - DSN 解析
- 嘉立创 EDA 专业版扩展 API 框架

#### 1.2.2 与现有代码的集成方式

所有算法优化可以直接在 `router.js` 中实现：

```javascript
// 示例：将 BFS 改为 A*
// 只需修改搜索队列和距离计算逻辑
class AStarRouter {
	constructor() {
		this.priorityQueue = new PriorityQueue();
	}

	heuristic(a, b) {
		// Manhattan 距离
		return Math.abs(a[0] - b[0]) + Math.abs(a[1] - b[1]);
	}

	// ... 其他实现
}
```

### 1.3 API 依赖分析

嘉立创 EDA 扩展 API 提供的能力：

| API                         | 用途        | 状态           |
| --------------------------- | ----------- | -------------- |
| `eda.sys_IFrame.openIFrame` | 打开 iframe | ✅ 已有        |
| `eda.pcb_Pcb`               | PCB 操作    | 可用           |
| `eda.sys_I18n`              | 国际化      | 可用           |
| 浏览器 Web Workers          | 并行处理    | 标准浏览器 API |

### 1.4 例程参考

项目本身就是基于 `pro-api-sdk` 例程开发的完整示例，证明了扩展开发的可行性。

---

## 二、GPU 加速方案可行性分析

### ✅ 可行性结论：技术可行（有条件）

### 2.1 核心模块分析

| 模块                    | 可行性  | 说明           |
| ----------------------- | ------- | -------------- |
| WebGPU 基础集成         | ✅ 可行 | 浏览器标准 API |
| 并行路径搜索 GPU 着色器 | ✅ 可行 | 计算着色器实现 |
| 批量碰撞检测            | ✅ 可行 | 计算着色器实现 |
| 距离场预处理            | ✅ 可行 | 计算着色器实现 |
| 兼容性回退              | ✅ 可行 | 纯 JavaScript  |

### 2.2 技术实现路径

#### 2.2.1 浏览器支持情况

| 浏览器       | WebGPU 支持            | 状态 |
| ------------ | ---------------------- | ---- |
| Chrome 113+  | ✅ 完整支持            | 推荐 |
| Edge 113+    | ✅ 完整支持            | 推荐 |
| Firefox 113+ | ✅ 完整支持（Nightly） | 可用 |
| Safari 17.0+ | ⚠️ 部分支持            | 可用 |

#### 2.2.2 与现有代码的集成

GPU 代码可以作为可选模块：

```javascript
// 检测 WebGPU 检测与现有代码集成
class GPURouter extends Router {
	constructor() {
		super();
		this.hasWebGPU = false;
	}

	async init() {
		try {
			const adapter = await navigator.gpu?.requestAdapter();
			if (adapter) {
				this.device = await adapter.requestDevice();
				this.hasWebGPU = true;
			}
		} catch (e) {
			console.warn('WebGPU 不可用，使用 CPU');
		}
	}

	async route() {
		if (this.hasWebGPU) {
			return this.routeGPU();
		} else {
			return super.route();
		}
	}
}
```

### 2.3 与嘉立创 EDA 环境兼容性

嘉立创 EDA 专业版是基于 Electron/Chrome 内核的桌面应用，理论上支持 WebGPU，但需要验证版本支持。

### 2.4 回退方案

GPU 方案设计了完整的回退机制：

- WebGPU 不可用时自动降级到 WebGL
- WebGL 也不可用时降级到纯 CPU
- 不影响现有功能

---

## 三、综合评估

### 3.1 两个方案对比

| 维度     | 布线优化方案  | GPU 加速方案     |
| -------- | ------------- | ---------------- |
| 开发难度 | 中等          | 较高             |
| 开发周期 | 1-2 个月      | 2-3 个月         |
| 预期收益 | 2-5x 性能提升 | 10-100x 性能提升 |
| 兼容性   | 100% 兼容     | 需要较新浏览器   |
| 调试难度 | 较低          | 较高             |
| API 依赖 | 无特殊依赖    | WebGPU（可选     |

### 3.2 推荐实施路径

\*\*阶段一：布线优化方案（优先）

1. A\* 搜索算法
2. 45 度角走线
3. 智能网络排序
4. 路径优化

\*\*阶段二：GPU 加速（可选，条件成熟时）

---

## 四、结论

### ✅ \*\*两个方案都可以通过现有的 API 和例程实现！

1. **布线优化方案**：完全可行，建议优先实施，可以获得立竿见影的效果。

2. **GPU 加速方案**：技术上可行，依赖浏览器 WebGPU 支持，建议作为第二阶段目标。

---

## 五、下一步行动

1. 实施布线优化方案（已在 `optimization-phase1` 分支
2. 验证嘉立创 EDA 浏览器内核版本
3. 测试 WebGPU 在目标环境可用性
