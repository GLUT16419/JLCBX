# 网络分组与批量处理功能问题分析

## 问题概述

在实现网络分组与批量处理功能后，布线功能完全失效，无法产生任何布线结果。

---

## 问题原因分析

### 1. 代码插入位置错误 🔴

**问题位置**: `routeWithVisited` 方法插入位置不正确

**原始代码结构**:

```javascript
// Net 类方法顺序
class Net {
    constructor(...) {}
    shuffle_topology() {}
    add_terminal_collision_lines() {}
    sub_terminal_collision_lines() {}
    route() { ... }  // ← 原布线方法
    // ... 其他方法
}
```

**插入后结构**:

```javascript
class Net {
    constructor(...) {}
    shuffle_topology() {}
    add_terminal_collision_lines() {}
    sub_terminal_collision_lines() {}
    routeWithVisited(...) {}  // ← 错误插入到了 route() 前面！
    route() { ... }  // 被挤到后面
    // ... 其他方法
}
```

**影响**: 虽然顺序不应该影响功能，但插入位置可能破坏了预期的代码结构。

---

### 2. 新方法引用未定义的变量 🔴

**问题代码**: `routeWithVisited` 方法中引用了未定义的函数

```javascript
routeWithVisited(globalVisited, localVisited) {
    // ...
    let routed = net.routeWithVisited(visited, netVisited);  // ❌ 这里有问题！
    // ...
}
```

**问题**: 在 `PCB.routeGroup` 中调用 `net.routeWithVisited()`，但这个方法的参数顺序可能不一致。

---

### 3. 方法实现与原方法不一致 🔴

**`routeWithVisited` 方法问题**:

| 原 `route()` 方法                           | 新 `routeWithVisited()` 方法                  |
| ------------------------------------------- | --------------------------------------------- |
| 使用内部 `visited` 变量                     | 接收外部 `globalVisited` 和 `localVisited`    |
| 调用 `this.backtrack_path(visited, ...)`    | 调用 `this.backtrack_path(localVisited, ...)` |
| 最后调用 `this.add_paths_collision_lines()` | 也调用了，但可能时机不对                      |

**关键差异**:

- 原方法的 `visited` 是在循环外定义的，每次迭代累积
- 新方法的 `localVisited` 是每次迭代新建的，可能导致状态不同步

---

### 4. 未使用的新方法 🔴

**问题**: 新增的 `routeWithGrouping()` 方法从未被调用！

```javascript
// 新增了这个方法，但原有的 route() 方法没有使用它
routeWithGrouping() {
    let groups = this.groupByType();
    // ... 分组布线逻辑
}
```

**影响**: 新增的分组逻辑完全没有生效，反而增加了代码复杂性。

---

### 5. 网络名依赖问题 🔴

**问题**: `isPowerNet`, `isClockNet`, `isCriticalNet` 依赖于 `net.m_name`

```javascript
isPowerNet(net) {
    if (!net.m_name) return false;  // ❌ 依赖未定义的属性
    // ...
}
```

**实际情况**: 当前的 `Net` 类没有 `m_name` 属性！网络名没有从 DSN 解析传递过来。

**影响**: 所有类型判断都返回 `false`，分组功能失效。

---

## 根本原因总结

| 问题类型       | 严重程度 | 影响           |
| -------------- | -------- | -------------- |
| 代码插入位置   | 中       | 可能破坏类结构 |
| 未定义变量引用 | 高       | 运行时错误     |
| 方法实现不一致 | 高       | 状态不同步     |
| 新方法未调用   | 高       | 功能完全无效   |
| 依赖未定义属性 | 高       | 分组判断失效   |

**核心问题**: 实现过于复杂，没有逐步测试，一次性引入了226行未经验证的代码。

---

## 修复方案建议

### 方案 1: 简化实现（推荐）

1. **分步实现**: 先实现分组判断逻辑
2. **验证基础功能**: 确保每个新方法都能单独工作
3. **逐步集成**: 在确认基础功能后再集成到主流程

### 方案 2: 最小化改动

1. **只添加必要的方法**: 删除 `routeWithVisited` 和 `routeGroup`
2. **使用现有接口**: 修改 `sortNetlistSmart()` 来实现分组排序
3. **保持原有布线流程**: 不改变核心布线逻辑

### 方案 3: 完整重写

1. **设计清晰的接口**: 定义明确的分组和批量处理 API
2. **编写单元测试**: 每个方法都有测试用例
3. **集成测试**: 在真实场景中验证

---

## 推荐实施路径

```
阶段 1: 验证网络名传递
  ↓
阶段 2: 实现类型判断函数（带空值检查）
  ↓
阶段 3: 修改排序逻辑使用类型信息
  ↓
阶段 4: 测试分组排序效果
  ↓
阶段 5: 实现批量处理（如果需要）
```

---

## 当前状态

✅ **已恢复到工作版本** - 移除了问题代码
❌ **分组功能待重新实现** - 需要更谨慎的方法

---

## 经验教训

1. **避免一次性大量改动**: 复杂功能应该分步实现和测试
2. **检查依赖关系**: 确保使用的属性和方法确实存在
3. **保持代码同步**: 新增方法应该与原有方法保持一致
4. **测试驱动开发**: 在编写代码前先写测试用例

---

## 后续建议

如果需要继续实现网络分组功能，建议：

1. 先修改 `dsn2pcb.js` 传递网络名
2. 在 `Net` 类中添加 `m_name` 属性
3. 测试类型判断函数
4. 修改排序逻辑使用类型信息
5. 最后再考虑批量处理功能
